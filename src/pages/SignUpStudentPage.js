import React from "react";
import PageContainer from "../components/PageContainer";

const SignUpStudentPage = (props) => {
  return <PageContainer title={"Sign up"}>Student</PageContainer>;
};

export default SignUpStudentPage;
